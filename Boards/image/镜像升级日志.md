v1.71:
1、提高x4颗粒的兼容性
2、新增教育版支持
3、修复复位LSD控制器，会把UART波特率复位成115200的问题。

版本V1.70:
1.OPT阈值上限依据芯片设置，工业级105°，其他85°

版本V1.69：
1.OTP阈值上限TL1固定85°C,OTP阈值下限TL2调整为软件提供接口给用户设置。
下限设置方法请参考附档《Processor Base Firmware接口规范V3.2》4.2.28章节

版本v1.68:
1.优化温度获取，新增温度校准机制
2.支持无盖版CPU
3.修复low power enable与fast training不能同时使用的BUG。

版本v1.67:
1、新增DDR服务，获取当前training状态，用于固化training参数。
2、更新DDR参数v0.6，新增低功耗配置，stream性能下降2%。cpu idle功耗下降10%，stream功耗下降2%。
(注意：此数据为公版测试数据，只作为参考，以客户主板实际测试数据为准)
3、更新DDR参数v0.6，使能刷新2X模式。新增2T preamable plus
4、优化内存training 参数遍历逻辑，更快找到正确training结果。
5、优化内存参考电压计算逻辑，支持一些信号较差内存。
6、澜起内存针对性优化初始化失败问题。
7、优化动态调频响应频率，加快调频。
8、优化负温显示。

版本V1.66：
1.新增MAC地址。（D2000+4CV2 总共新增800万个左右）
2.解决X100读CPU温度概率卡死的问题。

版本V1.65:
1.新增ssc展频设置服务。具体服务号参考《Processor Base Firmware接口规范v3.1》
2.双rank性能优化，多线程分数提升10%左右，配合打包工具配置，参考《D2000 固件编译打包说明V1.0.5》
3.新增SDEI支持。
4.新增过温保护功能（结温超过90°CPU主频会降低到1Ghz,结温降低到70°时主频恢复原值），默认打开

版本V1.64:
1.安全固件与通用固件合一
2.支持pcie port配置开关，PCIE参数版本号需要为4，参考《Processor Base Firmware接口规范v3.1》
3.新增DDR服务，硬件检错功能

版本V1.2:
1、限制CPU主频和DDR频率上限。
2、提高DDR稳定性

版本V1.3:
1.支持CPU型号读取
2.支持S3休眠唤醒
3.QSPI驱动能力默认值修正。

版本V1.4:
1.支持EFUSE类型读取（MAC地址）
2.新增支持澜起内存

版本V1.5:
1.解决OS调频问题导致的spec分数偏低，建议打开acpi表里的scpi描述

版本V1.6:
1.提升内存stream性能，需更新UEFI ，内存参数表配置成02

版本V1.62:
1.新增SCRUB 开关，DM使能开关（DDR参数表版本需改为0.3）
2.优化双通道内存初始化时间
3.ltp hotplug 检测BUG修复

版本V1.62-2:
1.配置工具升级至V0.2，PBF可通过菜单界面配置参数。

版本V1.63:
1.新增DDR服务：打印内存training 边界值
2.新增临终遗言功能。
3.新增uart打印级别控制，PBF LOG暂存功能。
4.新增PCIE 参数。
5.新增DDR服务，支持scrub空间配置

版本V1.63.1:
新增MAC地址范围

